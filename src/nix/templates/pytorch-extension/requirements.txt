# PyTorch CPU-only index
--extra-index-url https://download.pytorch.org/whl/cpu

# PyTorch and core dependencies (CPU-only)
torch>=2.1.0
numpy>=1.21.0

# Testing dependencies
pytest>=7.0.0
pytest-benchmark>=4.0.0

# Build dependencies  
setuptools>=45.0.0
wheel
