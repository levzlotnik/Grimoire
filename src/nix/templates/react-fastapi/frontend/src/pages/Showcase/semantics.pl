% Showcase pages directory semantics
% UI component showcase system

:- self_entity(react_fastapi_template(frontend(src(pages(showcase))))).

% Showcase directory docstring
docstring(react_fastapi_template(frontend(src(pages(showcase)))), "UI component showcase system demonstrating various interface elements and patterns").

% Showcase categories
component(react_fastapi_template(frontend(src(pages(showcase)))), category, alerts).
component(react_fastapi_template(frontend(src(pages(showcase)))), category, buttons).
component(react_fastapi_template(frontend(src(pages(showcase)))), category, cards).
component(react_fastapi_template(frontend(src(pages(showcase)))), category, feedback).
component(react_fastapi_template(frontend(src(pages(showcase)))), category, forms).
component(react_fastapi_template(frontend(src(pages(showcase)))), category, modals).
component(react_fastapi_template(frontend(src(pages(showcase)))), category, navigation).
component(react_fastapi_template(frontend(src(pages(showcase)))), category, tables).