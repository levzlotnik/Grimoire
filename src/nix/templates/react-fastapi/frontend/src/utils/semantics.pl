% Frontend utils directory semantics
% Utility functions and helpers

:- self_entity(react_fastapi_template(frontend(src(utils)))).

% Utils directory docstring
docstring(react_fastapi_template(frontend(src(utils))), "Utility functions and helper modules (currently empty, available for future utilities)").